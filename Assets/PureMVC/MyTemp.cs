using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MyTemp : MonoBehaviour
{
  
    void Start()
    {
        ReaderSkillConfig _readerSkillConfig = new ReaderSkillConfig("Configs/SkillConfig");
    }

    // Update is called once per framed
    void Update()
    {
        
    }
}
